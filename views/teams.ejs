<!DOCTYPE html>
<html>
  <head></head>
  <body>
    <div>
      <a href="/addTeam">チームを追加</a>
    </div>
    <% if(teams.length > 0){ %>
    <div align = "center">
      <% teams.forEach((team)=>{ %>
        <li><a  href = "http://localhost:3000/<%= team.id %>"><%= team.name %></a></li>
      <% }); %>
    </div>
    <% } %>
  </body>
</html>